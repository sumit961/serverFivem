# FXServer Network
endpoint_add_tcp "0.0.0.0:30120"
endpoint_add_udp "0.0.0.0:30120"

# Server Info
sv_hostname "Grand RP by Sumit_Yadav"
sets sv_projectName "Grand RP Custom"
sets sv_projectDesc "CM-Core Framework - Built by Sumit_Yadav"
sets tags "rp, custom, cm-core"
sets locale "en-US"
sv_maxclients 10

# Game Build
sv_enforceGameBuild 3258

# Local secrets (license key, Steam API key, MySQL connection string, etc.)
# Values live in server.local.cfg, which is gitignored and never tracked by git.
exec server.local.cfg

set cm_environment "development"

# System Chat
set resources_useSystemChat true

# Admin Setup
add_ace group.admin command allow
add_ace group.admin command.quit deny
add_principal identifier.fivem:1494046 group.admin
add_ace group.clothingadmin cm.clothing.admin allow
add_ace group.admin cm-house.create allow

add_principal identifier.fivem:1494046 group.clothingadmin

add_ace group.admin cm-house.admin allow
add_ace group.admin cm-house.admin.properties allow
add_ace group.admin cm-house.admin.interiors allow
add_ace group.admin cm-house.admin.garages allow
add_ace group.admin cm-house.admin.pricing allow
add_ace group.admin cm-house.admin.photos allow
add_ace group.admin cm-house.admin.recovery allow

add_ace group.admin op_clothing.admin allow

# REQUIRED CFX Resources
ensure sessionmanager
ensure hardcap

# LIBRARIES FIRST
ensure ox_lib
ensure ox_target
ensure bcrypt
ensure oxmysql
ensure bob74_ipl

# OPTIONAL / UTILITY
# Rename folder from screenshot-basic-master to screenshot-basic if possible
ensure screenshot-basic

#ensure op-clothing

# CM CORE FIRST
ensure cm-ui
ensure cm-core

# LOGIN / PLAYER LOAD
ensure cm-auth

ensure cm-playerdata
ensure cm-characters

ensure cm-admin
ensure cm-spawn
ensure cm-climatime
ensure cm-population

# ITEMS / INVENTORY
ensure cm-items
ensure cm-inventory
ensure cm-itemactions

# UI / HUD
ensure cm-hud
ensure cm-chat

# STORES / CLOTHING

#ensure cm-clothstore
#ensure cm-clothingadmin
# These legacy replacement packs total roughly 9 GB and compete with addon
# clothing for the same freemode streaming budget. Keep them disabled while
# validating the collection-based zpack_1 addon; merge/optimise them before use.
#ure eup-stream
#nsure sClothesV9
#nsure zpack_1
ensure nv_cloth
ensure cm-weapons

ensure cm-store


ensure energy_hpsandyshores
ensure map4all-pillbox

# VEHICLES
ensure vStudios_PacificBluffsDealership_Ultimate
#ensure 600-DebadgedCars

ensure cm-vehiclekeys
ensure cm-vehicles
ensure cm-gasstations
ensure cm-carwash

#ensure cm-vehiclestore
ensure cm-tuning
ensure rn-vehicleshop
ensure cm-parking

# Disabled: directory exists locally but has no fxmanifest.lua/__resource.lua.
# Add a vendor-provided manifest only after its required data_file declarations
# are known and validated.
# ensure gn_dual_mediccenter

ensure cm-house
ensure cm-family
ensure cm-ems
ensure cm-law
ensure cm-police
ensure cm-gunstore
ensure cm-doctor
ensure prisonprops
ensure int_prisonfull
ensure cm-prison
