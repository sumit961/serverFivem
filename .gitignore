# FiveM cache / generated files
cache/
txData/
crashes/
logs/
*.log
server.log

# Private local server configs / secrets
server.local.cfg
*.local.cfg
server.cfg.bkp
.env
.env.*
!.env.example
*.key
*.pem

# Local security backup snapshots (never tracked)
local-security-backup/

# Local embedded database state (never commit live data)
db/default/

# Large/private asset folders
resources/\[mlo\]/
resources/\[clothes\]/

# Safe placeholder template remains tracked.
!server.local.example.cfg

# CM runtime controller local configuration/state
tools/cm-runtime/runtime.local.json
tools/cm-runtime/logs/
tools/cm-runtime/STOP

# Backup zip files
*.zip

# Graphify generated output
graphify-out/

# cm-fivem-map generated output
cm-agent-out/
__pycache__/
*.pyc
