# S-inventory — weapons

Paste the snippet below inside `es_extended/shared/weapons.lua` (inside the `Config.Weapons` table).

```lua
{ name = 'WEAPON_RADAR', label = 'Radar Gun', components = {} },
{ name = 'WEAPON_FLASHBANG', label = 'Flashbang', components = {} },
```
