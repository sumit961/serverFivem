# Validation completed

- Lua syntax parsed for `fxmanifest.lua`, `shared/config.lua`, `client/main.lua` and `server/main.lua`.
- JavaScript passed `node --check`.
- Every JavaScript NUI request has a matching client callback.
- Every client server-event call has a matching server handler.
- Every server client-event call has a matching client handler.
- No CSS backdrop filters are used.
- No client-submitted purchase price remains.
- ZIP integrity checked after packaging.

A live FiveM test is still required for entity ownership, OneSync natives, database exports and the exact vehicle models installed on the server.
