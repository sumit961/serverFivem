# CM Bank v2.0.1 — NUI Visibility and Stylesheet Fix

- Restored the established `ui/style.css` filename for FiveM packfile compatibility.
- Added an inline ARIA safety gate that hides the bank before external CSS loads.
- Added direct JavaScript display control when opening and closing the bank.
- Added the same fail-safe display control to the teller interaction prompt.
- The bank can no longer expose raw HTML or full-size decorative images while closed.
