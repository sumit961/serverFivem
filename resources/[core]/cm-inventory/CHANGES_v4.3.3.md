# cm-inventory v4.3.3

- Adds `medikit` as a usable medical item with the existing medkit image.
- Keeps the legacy `medkit` item available for existing inventories.
- Adds matching use cooldown and progress configuration.
