# cm-family v1.1.8 — cm-chat integration

- Family chat payload includes the sender source for server-side verification.
- `SendFamilyChat` returns explicit success/failure.
- Default GTA chat fallback is suppressed while `cm-chat` is running.
- cm-chat channels refresh immediately after family state changes.
- `/f` and `/familychat` remain available.
