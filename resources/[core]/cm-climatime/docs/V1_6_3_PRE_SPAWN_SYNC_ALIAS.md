# CM Climatime v1.6.3

Fixes:
- Restores valid `fx_version 'cerulean'`.
- Adds `cm-climatime:server:requestPreSpawnClimate` used by cm-spawn.
- Adds `cm-climatime:client:applyBeforeSpawn` alias.
- Allows spawn page climate preload before the player clicks a spawn card.
