# rn-vehicleshop v3.0.1 family image export

- Added server export `GetVehicleImage(model)`.
- Returns the exact `cm_vehicle_catalog.image` value used by the shop UI.
- Supports hidden catalog rows so family/vehicle UIs can still show an existing captured image.
