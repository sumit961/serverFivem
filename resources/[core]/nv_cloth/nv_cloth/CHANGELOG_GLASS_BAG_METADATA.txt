CM Glassmorphism + Bag Metadata Update

Files changed:
- web/index.html
- web/style.css
- server/sv_cloth.lua

UI:
- Added glassmorphism panels with blur, border, shine and soft shadow.
- Left and right panels are vertically centered with equal top/bottom gap.
- Store panel is compact and no longer sits at a strange top offset.
- Admin panel stays full height and remains usable as a creator dashboard.
- Improved category buttons, selectors, buttons, admin fields and texture pills.
- Added CSS/JS cache-busting version glass-v2.

Bags:
- Bag admin level selector labels are simplified to Level 1-4 only.
- nv_cloth now saves only bagLevel for bag metadata.
- Removed per-item backpackSlots/maxWeight/slots/weight duplicate metadata from new bag saves.
- Inventory should resolve bag capacity from bagLevel using its own BagLevels config.
