# cm-house v1.7.24 — Physical slot detection

- Hides an occupied marker only while its actual network vehicle is within three metres of the slot.
- Shows the reserved red marker immediately when a vehicle is driven away inside the garage.
- Uses live entity position instead of stale database placement state.
