# cm-house v1.8.1

## Opaque family-matched UI

- Replaced translucent house cards, rows, controls, panes, and toolbars with solid navy surfaces matching `cm-family`.
- Added local fallback values for every shared dashboard color, shadow, gradient, and font token.
- Prevented transparent rendering when `cm-ui` is stale, cached, or temporarily unavailable during resource startup.
- Matched the family overlay, Poppins display typography, DM Sans body typography, blue actions, and cyan highlights.

No NUI callback, permission, database, or gameplay behavior changed.
