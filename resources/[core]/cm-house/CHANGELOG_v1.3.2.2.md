# cm-house v1.3.2.2

- Fixed garage layout placement vehicles being permanently blocked by `cmConditionReady`.
- Placement cars spawned through `cm-vehicles` are now immediately driveable when requested with `engineOn = true`.
- No SQL migration is required.
