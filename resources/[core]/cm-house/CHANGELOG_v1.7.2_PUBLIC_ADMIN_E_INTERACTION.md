# cm-house v1.7.2

- Reinforced `Config.DevelopmentPublicAdmin = true` directly inside the admin panel gate.
- Added unique public-admin command aliases: `/cmadminhouse`, `/cmhouseadmin`, and `/houseadmin`.
- Kept `/cmadmin` for compatibility.
- Added a startup warning listing the public commands while the bypass is enabled.
- Restored a clean inline cyan `E` before every complete interaction label.
- Kept the interaction background transparent and removed truncation.
- Bumped NUI CSS/JS filenames to avoid stale FiveM cache.
